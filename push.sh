# This script is used to perform git operations on the repository.
# The commands are stage, commit, and push to the main branch of the remote repository.

# Stage all files in the repository
git add .

# Commit the staged files
git commit -m "Update"

# Push the committed files to the main branch of the remote repository
git push origin main